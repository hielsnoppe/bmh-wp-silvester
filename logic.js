import { shopProducts } from "./data/products.js";

window.addEventListener("DOMContentLoaded", () => {
    const logo =document.querySelector('header nav nav-mid h1')
});
for (product of shopProducts) {
    console.log(product);
}
